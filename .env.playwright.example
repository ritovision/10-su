# Copy this file to `.env.playwright` at the repo root and provide the values below.
# These environment variables are consumed by the Playwright tests under `nodejs/playwright`.

# Private key (0x-prefixed; use a throwaway key)
PRIVATE_KEY=

# Network configuration
NETWORK=Sepolia
CHAIN_ID=11155111
RPC_URL=https://rpc.sepolia.org

# Base URL to visit when running tests locally (matches the Jekyll dev server below)
TEST_BASE_URL=http://127.0.0.1:4000

# Optional overrides
WALLET_NAME=Test Wallet
TX_DELAY_MS=2000
MCP_ENDPOINT=/api/mcp
